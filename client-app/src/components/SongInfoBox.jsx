// import React from 'react'

// const SongInfoBox = ({ data }) => {
//   return (
//     <>
//       {data.songs.length > 0 ? (
//         <div className='min-w-36 w-36 max-w-36'>
//           {data.songs.map((song) => (
//             <div key={song.id} song={song} />
//           ))}
//         </div>
//       ) : (
//         <p>no songs</p>
//       )}
//     </>
//   )
// }

// export default SongInfoBox
